# Lectern in use

The user rejected the slideshow and requested an ad-like video showing someone using Lectern, with zooms and different actions. Preserve the forest-green identity and use genuine app UI.

32-second, 1600 × 1000 film. A continuous camera follows course selection, opening a lecture, opening Generate, toggling flashcards, reviewing notes, scrolling the live native notes view, switching to Cards, and opening a card editor. Close on the download invitation. Click rings indicate actions without a cursor baked into the images. The generation-to-notes edit is labeled as previously generated materials. Do not claim an actual new generation or Anki sync occurred.

Assets are new cursor-free window captures. `notes-live.mp4` records actual native scrolling. The composition crops its top 30 pixels and right 26 pixels to exclude the recording indicator and system cursor. App behavior was verified in LectureDetailView.swift, GenerateSheet.swift, FlashcardsView.swift, and assets/demo.js. The app's generation provider was restored after capturing its choices; no generation or card save was submitted.
