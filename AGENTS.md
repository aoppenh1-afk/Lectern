# Agent notes

Install and first-launch setup live in [README.md](README.md#install-this-repo-for-the-user). Follow that section when someone asks you to install Lectern, clone this repo, or get the app running on their Mac.

Build, test, and release commands are in `scripts/` and `project.yml`. After changing `project.yml`, run `xcodegen generate`.
